# CCA108 Historical Evidence:

`before_spearman_fix/` preserves the measured HTTP 500 Spearman results from the first benchmark run.

The successful Pearson and Spearman results are stored in the main `aws/` and `traffic/` folders.
